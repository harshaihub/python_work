#Example: NumPy
#NumPy is used for numerical computing (arrays, matrices, math functions).

import numpy as np

arr = np.array([1, 2, 3, 4])
print(arr * 2)   # Output: [2 4 6 8]

#Example: Pandas
#Pandas is used for data analysis and manipulation (tables, CSV files, datasets).

import pandas as pd

data = {"Name": ["Vivek", "Shreyas", "Sanmet"], "Marks": [85, 90, 78]}
df = pd.DataFrame(data)

print(df)
